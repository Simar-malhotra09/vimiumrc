[project]
name = "phone-remote"
version = "0.1.0"
description = "Use an iPhone as a wireless scroll trackpad for macOS."
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.9",
    "pynput>=1.7",
]
